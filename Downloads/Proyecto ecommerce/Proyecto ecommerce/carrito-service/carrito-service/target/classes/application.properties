# =============================================
# Microservicio: carrito-service
# Puerto: 8083
# =============================================
spring.application.name=carrito-service
server.port=8083

# =============================================
# Conexión Oracle con Wallet
# =============================================

spring.datasource.url=jdbc:oracle:thin:@ecommerce_high?TNS_ADMIN=src/main/resources/wallet
spring.datasource.username=CARRITO_USER
spring.datasource.password=Carritouser123
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# =============================================
# JPA / Hibernate
# =============================================

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# =============================================
# JWT — debe coincidir con autenticacion-service
# Mínimo 32 caracteres para HS256
# =============================================
jwt.secret=clave-secreta-compartida-ecommerce-2024-muy-larga
